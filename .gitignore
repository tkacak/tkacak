# API keys — must never enter the repository
.Renviron

# R working files
.Rhistory
.RData
.Rproj.user/

# Data and outputs (raw LLM responses can grow large; remove the
# data/raw/ line if you want to version the responses)
data/raw/
data/processed/
data/real/
output/
